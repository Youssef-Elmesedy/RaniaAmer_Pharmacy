@model OfferFormViewModel
@{
    ViewData["Title"] = "إضافة عرض جديد";
}

@section Styles {
    <link rel="stylesheet" href="~/css/pages/admin-offers.css" asp-append-version="true" />
}

<div class="d-flex flex-column flex-sm-row justify-content-between align-items-start align-items-sm-center gap-3 mb-4">
    <div>
        <h3 class="admin-page-title mb-1">إضافة عرض جديد</h3>
        <p class="text-muted small mb-0">قم بتحديد الأصناف وتحديد أسعارها المخفضة لإنشاء عرض جديد</p>
    </div>
    <a asp-action="Index" class="btn btn-sm btn-outline-secondary">
        <i class="fa-solid fa-arrow-right me-1"></i> رجوع للعروض
    </a>
</div>

<div class="card border-0 shadow-sm p-4 rounded-4">
    <form asp-action="Create" method="post">
        @await Html.PartialAsync("_OfferForm", Model)
    </form>
</div>